# Production Environment Variables for Backend (Railway/Render)
# Copy these to Railway Dashboard → Variables tab

# Node Environment
NODE_ENV=production

# Server Configuration
PORT=3000

# Database Configuration
# Option 1: Railway PostgreSQL (automatically injected)
DATABASE_URL=postgresql://user:password@host:port/database

# Option 2: Individual database credentials (if not using Railway)
# DB_HOST=your-postgres-host.com
# DB_PORT=5432
# DB_NAME=ostly_db
# DB_USER=ostly_user
# DB_PASSWORD=your-secure-password

# CORS Configuration (replace with your Vercel frontend URL)
CORS_ORIGIN=https://your-app.vercel.app
FRONTEND_URL=https://your-app.vercel.app

# Security (generate strong secrets for production)
# Future: JWT Authentication (when implemented)
# JWT_SECRET=your-super-secret-jwt-key-min-32-chars
# JWT_EXPIRES_IN=7d

# Future: File Storage (when implemented)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_S3_BUCKET=
# AWS_REGION=us-east-1

# Future: Email (when implemented)
# EMAIL_FROM=noreply@ostly.app
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASSWORD=your-sendgrid-api-key

# Monitoring (optional)
# SENTRY_DSN=
# LOG_LEVEL=info



